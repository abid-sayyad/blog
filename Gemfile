source "https://rubygems.org"
gemspec

# Ruby 3.0+ dropped webrick from the standard library; jekyll serve needs it.
gem "webrick", "~> 1.8"

# Efficient file-change watching on Windows (avoids slow polling fallback).
gem "wdm", "~> 0.1", platforms: :windows
